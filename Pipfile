[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
numpy = ">=1.24"
# mlx is Apple Silicon only; install manually: pip install mlx
# mlx = {version = ">=0.10", markers = "sys_platform == 'darwin' and platform_machine == 'arm64'"}

[dev-packages]
pytest = ">=7.0"
pytest-cov = ">=4.0"
mypy = ">=1.0"
numpy = ">=1.24"
matplotlib = ">=3.0"
scipy = ">=1.10"

[requires]
python_version = "3.12"
